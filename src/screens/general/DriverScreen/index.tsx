import React from 'react';
import {Text} from 'react-native';
import {styles} from './styles';

const DriverScreen = () => {
  return <Text style={styles.textColor}>For User Screen</Text>;
};

export default DriverScreen;
